package com.example.vitbatch1.kotlinexs

class Teacher(var name: String, var age: Int, var postalAddress: String)

class Department(var name: String, var age: Int, var postalAddress: String)

class College(var name: String, var age: Int, var postalAddress: String)

class Studio(var name: String, var age: Int, var postalAddress: String)
